name: kaptis
description: "Kaptis - A strategic board game."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.4
  ndk: ^0.7.1-dev.12
  ndk_flutter: ^0.0.2-dev.4
  app_links: ^7.0.0
  share_plus: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
